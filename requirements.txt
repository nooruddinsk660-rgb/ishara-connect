# Core web / realtime
Flask==3.1.3
Flask-SocketIO==5.6.1
eventlet==0.36.1
gunicorn==23.0.0
python-dotenv==1.0.1

# Computer vision / ML
opencv-python==4.11.0.86
mediapipe==0.10.14
# IMPORTANT, verified in a clean venv while building this fix: mediapipe 0.10.33
# (the current latest 0.10.x at time of writing) has REMOVED `mp.solutions`
# entirely on Python 3.12 -- `import mediapipe; mediapipe.solutions` raises
# AttributeError. 0.10.14 is the newest version confirmed to still ship the
# legacy `mp.solutions.hands` / `drawing_utils` API this app depends on
# throughout app.py and data_collector.py. Do not bump this without first
# confirming `python -c "import mediapipe as mp; mp.solutions.hands.Hands()"`
# still works -- Google is actively removing this API in favor of the newer
# Tasks API (mp.tasks.vision.HandLandmarker), and it may vanish from a given
# version/platform combo with no warning.
scikit-learn==1.5.2
numpy==1.26.4
pandas==2.2.3
# mediapipe 0.10.14's own dependency resolution (confirmed in a clean venv):
protobuf==4.25.9
attrs==26.1.0

# Data layer (Phase 1 -- Postgres transcript logging, optional/additive)
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.13.3

# Text-to-speech
gTTS==2.5.4
edge-tts==7.2.8

# Training-time only (train_model.py's confusion-matrix plot)
seaborn==0.13.2
matplotlib==3.9.2

# NOTE on testing: mediapipe==0.10.14 + opencv-python + utils.py's
# extract_keypoints() were verified together in a clean venv (hands.process()
# on a synthetic frame -> correct 126-d zero-filled vector). Flask/Socket.IO/
# the browser video path were NOT tested here -- no camera or browser in this
# environment. Install, run the actual app locally, and once confirmed
# working end to end, lock everything for real with:
#   pip freeze > requirements.txt